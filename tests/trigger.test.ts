import { DispatcherEngine, DispatchableEvent, DispatchableListener } from '../src/index';

test('Init Dispatchable Event', () => {
    // const dispatcher = new DispatcherEngine();
    // const event = new DispatchableEvent('add-member');
    // const firstListener = new DispatchableListener(() => {
    //     return;
    // });
    // const secondListener = new DispatchableListener(() => {
    //     return;
    // }); 

    // event.setListeners([firstListener, secondListener]);
    // dispatcher.setEvents([event]).register();

    // dispatcher.fire('add-member');
})